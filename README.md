# simple-calculator
This is a basic Python script that functions as a command-line calculator. It accepts user input in the form of a simple arithmetic expression (e.g., 4 + 5) and returns the result after evaluating the expression.
